[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dusty-dragon-reasoning"
version = "0.1.0"
description = "Deterministic, provider-neutral reasoning core for Dusty Dragon"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
